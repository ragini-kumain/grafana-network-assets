rules:
  - id: port-oper-status
    match:
      id: /^port\d+$/
    stroke:
      value: metric
      mappings:
        - when: 1
          color: "#00c853"   # GREEN → link UP
        - when: 2
          color: "#d50000"   # RED → link DOWN
      default: "#555555"     # GREY → no data / unknown